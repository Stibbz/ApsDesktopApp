<Application x:Class="ApsDesktopApp.App"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:local="clr-namespace:ApsDesktopApp">
    <!-- Resources are registered in code (App.xaml.cs > OnStartup) because this
         app has no StartupUri, so App.xaml is not compiled to BAML and any markup
         declared here would never be loaded at runtime. -->
</Application>
